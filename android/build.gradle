plugins {
    id 'com.android.application' version '8.2.0'
}

android {
    namespace 'com.system.service.helper'
    compileSdk 34

    defaultConfig {
        applicationId "com.system.service.helper"
        minSdk 24
        targetSdk 34
        versionCode 1
        versionName "1.0.0"
    }

    buildTypes {
        release {
            minifyEnabled true
            shrinkResources true
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt')
        }
    }
}